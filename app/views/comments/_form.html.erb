<%= form_with(model: [post, comment], url: post_comments_path(@post) ) do |f| %>
	<div class="">
	    <%= f.text_area :content,size: "50×30",placeholder: "コメントを入力" %>
	</div>
	<%= f.submit "コメントする" %>
<% end %>